[project]
name = "telegran-bot-selling-notebook"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot==21.10",
    "python-dotenv==1.0.1",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
